# Project workflow memory

- After completing any development change, rebuild and launch the Development version of the macOS app with `scripts/dev-rebuild-restart.sh`, then verify that the app process and development backend start successfully. A successful compile alone is not considered sufficient testing.
